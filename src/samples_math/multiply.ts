/**
 * A basic multiply function
 * @param n1 The multiplicand number
 * @param n2 The multiplier number
 * @returns The product of the multiplication
 */
const multiply = (n1: number, n2: number): number => {
  return n1 * n2
}

export { multiply }
