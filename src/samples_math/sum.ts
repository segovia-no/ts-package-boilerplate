/**
 * A basic sum function
 * @param n1 The base number
 * @param n2 The number to be added to the base number
 * @returns The sum of both numbers provided
 */
const sum = (n1: number, n2: number): number => {
  return n1 + n2
}

export { sum }
